.DS_Store
.env

test/
raw/


# Local config — may contain personal data (names, email addresses, user IDs)
settings.json
config.json
user_mapping.json
space_mapping.json

# OAuth tokens and credentials — never commit these
*.token.json
google-*-token.json
credentials.json

.venv/
__pycache__/
*.pyc